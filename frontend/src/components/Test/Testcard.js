import React from "react";
import "./tedst.css"; // Import the CSS file for styling

const Card = ({ image, title, description, buttonText, onButtonClick }) => {
  return <div className="card bg-white w-[300px] h-[200px]"></div>;
};

export default Card;
